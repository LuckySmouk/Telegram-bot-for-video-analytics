# 🎬 Telegram-бот для аналитики видео данных

Бот для анализа статистики видео-контента с использованием запросов на естественном языке (русский).

## 📋 Возможности

- ✅ Обработка вопросов на русском языке
- ✅ Автоматическая генерация SQL-запросов через LLM
- ✅ Поддержка различных типов запросов (количество, рост, фильтрация)
- ✅ Работа с датами в естественном формате
- ✅ Векторная база знаний для контекста (ChromaDB)

## 🛠 Технологический стек

- **Python 3.9+**
- **PostgreSQL** - основная база данных
- **ChromaDB** - векторная база для схемы БД
- **Ollama** - локальная LLM для генерации SQL
- **aiogram 3.x** - Telegram Bot Framework

## 📦 Установка

### 1. Клонирование и установка зависимостей

```bash
# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt
```

### 2. Установка PostgreSQL

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install postgresql postgresql-contrib
sudo systemctl start postgresql
```

**Windows:**
Скачайте с [postgresql.org](https://www.postgresql.org/download/windows/)

**MacOS:**
```bash
brew install postgresql@15
brew services start postgresql@15
```

### 3. Установка Ollama

**Linux:**
```bash
curl -fsSL https://ollama.com/install.sh | sh
```

**Windows/MacOS:**
Скачайте с [ollama.ai](https://ollama.ai/download)

**Запуск Ollama:**
```bash
ollama serve  # В отдельном терминале
```

**Загрузка моделей:**
```bash
ollama pull qwen2.5:7b
ollama pull nomic-embed-text
```

### 4. Настройка базы данных

```bash
# Войдите в PostgreSQL
sudo -u postgres psql

# Создайте пользователя и базу (или используйте существующие)
CREATE USER postgres WITH PASSWORD 'postgres';
CREATE DATABASE video_analytics OWNER postgres;
GRANT ALL PRIVILEGES ON DATABASE video_analytics TO postgres;
\q
```

### 5. Конфигурация

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
nano .env
```

Заполните параметры:

```env
# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=video_analytics
DB_USER=postgres
DB_PASSWORD=your_password

# Telegram Bot
TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather
```

**Получение токена бота:**
1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен в `.env`

## 🚀 Запуск

### Шаг 1: Загрузка данных

Убедитесь, что файл `video_data.json` находится в корневой директории проекта.

```bash
python json_to_base.py
```

**Ожидаемый вывод:**
```
✅ Успешно прочитан JSON-файл: video_data.json
   Найдено видео: 1500
✅ База данных 'video_analytics' создана
✅ Таблицы созданы
✅ Добавлено 1500 видео
✅ Добавлено 36000 снапшотов
✅ ChromaDB коллекция 'video_schema' создана с 1 записью
✅ ВСЕ ЭТАПЫ УСПЕШНО ЗАВЕРШЕНЫ!
```

### Шаг 2: Тестирование системы

```bash
python gen_model.py
```

Попробуйте задать тестовые вопросы:
- "Сколько всего видео есть в системе?"
- "Сколько видео набрало больше 50000 просмотров?"

### Шаг 3: Запуск бота

```bash
python telegram_bot.py
```

**Ожидаемый вывод:**
```
🤖 TELEGRAM-БОТ ДЛЯ АНАЛИТИКИ ВИДЕО ДАННЫХ
======================================================================

Бот запускается...
Для остановки нажмите Ctrl+C

✅ Подключено к PostgreSQL: video_analytics
✅ ChromaDB коллекция загружена: 1 записей
✅ Система аналитики успешно инициализирована
✅ Бот полностью инициализирован и готов к работе
📡 Запуск polling...
```

## 💬 Использование бота

### Команды

- `/start` - Приветствие и краткая справка
- `/help` - Подробная справка по использованию
- `/examples` - Примеры вопросов
- `/status` - Проверка работы системы

### Примеры вопросов

1. **Общая статистика:**
   ```
   Сколько всего видео есть в системе?
   ```

2. **По креатору:**
   ```
   Сколько видео у креатора с id creator_123 вышло с 1 по 5 ноября 2025?
   ```

3. **По метрикам:**
   ```
   Сколько видео набрало больше 100000 просмотров?
   ```

4. **Рост метрик:**
   ```
   На сколько просмотров в сумме выросли все видео 28 ноября 2025?
   ```

5. **Активность:**
   ```
   Сколько разных видео получали новые просмотры 27 ноября 2025?
   ```

## 📊 Структура базы данных

### Таблица `videos`
| Поле | Тип | Описание |
|------|-----|----------|
| id | TEXT | ID видео |
| creator_id | TEXT | ID создателя |
| video_created_at | TIMESTAMP | Дата публикации |
| views_count | INTEGER | Итоговые просмотры |
| likes_count | INTEGER | Итоговые лайки |
| comments_count | INTEGER | Итоговые комментарии |
| reports_count | INTEGER | Итоговые жалобы |

### Таблица `video_snapshots`
| Поле | Тип | Описание |
|------|-----|----------|
| id | TEXT | ID снапшота |
| video_id | TEXT | Ссылка на videos.id |
| views_count | INTEGER | Просмотры на момент |
| delta_views_count | INTEGER | Прирост просмотров |
| created_at | TIMESTAMP | Время замера |

## 🔧 Troubleshooting

### Ошибка подключения к PostgreSQL

**Проблема:** `psycopg2.OperationalError: could not connect to server`

**Решение:**
```bash
# Проверьте статус PostgreSQL
sudo systemctl status postgresql

# Перезапустите если нужно
sudo systemctl restart postgresql

# Проверьте параметры в .env
```

### Ошибка Ollama

**Проблема:** `Connection refused` при обращении к Ollama

**Решение:**
```bash
# Убедитесь что Ollama запущена
ollama serve

# В другом терминале проверьте модели
ollama list

# Если модель отсутствует
ollama pull qwen2.5:7b
```

### ChromaDB не найдена

**Проблема:** `Collection 'video_schema' not found`

**Решение:**
```bash
# Удалите старую базу
rm -rf ./chroma_db

# Пересоздайте
python json_to_base.py
```

### Бот не отвечает

**Проблема:** Бот не реагирует на сообщения

**Решение:**
1. Проверьте токен в `.env`
2. Проверьте логи: `tail -f telegram_bot.log`
3. Проверьте статус командой `/status`

## 📝 Логирование

Логи сохраняются в файлы:
- `telegram_bot.log` - логи бота
- `video_analytics.log` - логи системы аналитики

Просмотр логов в реальном времени:
```bash
tail -f telegram_bot.log
```

## 🧪 Тестирование

Для автоматического тестирования можно использовать скрипт:

```python
# test_queries.py
from gen_model import VideoAnalytics

analytics = VideoAnalytics()

test_questions = [
    "Сколько всего видео есть в системе?",
    "Сколько видео набрало больше 50000 просмотров?",
    "На сколько просмотров выросли видео 1 ноября 2025?"
]

for q in test_questions:
    print(f"❓ {q}")
    print(f"✅ {analytics.process_question(q)}\n")
```

## 📄 Лицензия

Этот проект создан для тестового задания.

## 👤 Автор

Создано для демонстрации работы с:
- PostgreSQL
- LLM (Ollama)
- Vector Databases (ChromaDB)
- Telegram Bot API (aiogram)
- Natural Language Processing

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте все зависимости установлены
2. Убедитесь что PostgreSQL и Ollama запущены
3. Проверьте логи на ошибки
4. Убедитесь что `.env` заполнен корректно